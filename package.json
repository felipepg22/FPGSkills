{
  "name": "fpgskills",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Portable agents and skills for AI coding tools.",
  "workspaces": [
    "agents/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=22.20.0"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "generate": "npm run generate --workspace @fpgskills/task-executor",
    "test": "npm run test --workspaces --if-present && node --test tests/*.test.mjs tests/performance-testing/*.test.ts",
    "validate": "npm run validate --workspaces --if-present && node scripts/validate-skill-payload.mjs",
    "test:skills:copy": "node scripts/smoke-skills-install.mjs copy",
    "test:skills:symlink": "node scripts/smoke-skills-install.mjs symlink",
    "pack:check": "npm pack --dry-run --workspace @fpgskills/task-executor",
    "publish:task-executor": "npm run test --workspace @fpgskills/task-executor && npm publish --workspace @fpgskills/task-executor --access public"
  },
  "license": "MIT"
}
