{
  "name": "fpgskills",
  "version": "1.0.0",
  "private": true,
  "description": "Portable agents and skills for AI coding tools.",
  "workspaces": [
    "agents/*",
    "skills/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "generate": "npm run generate --workspace @fpgskills/task-executor",
    "test": "npm run test --workspaces --if-present",
    "validate": "npm run validate --workspaces --if-present",
    "pack:check": "npm pack --dry-run --workspace @fpgskills/task-executor && npm pack --dry-run --workspace @fpgskills/fpg-agents-md-writer && npm pack --dry-run --workspace @fpgskills/implementation-spec-writer"
  },
  "license": "MIT"
}
