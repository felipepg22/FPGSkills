# Task

Enable compact output in `src/config.js`.

## Completion condition

The exported configuration contains `compact: true` while preserving the existing `mode` value.

## Constraints

- Change only `src/config.js`.
- Do not create or modify tests.
- Do not perform Git writes.
