# Task

Change the greeting exported by `src/greeting.js` from `Hello` to `Welcome`.

## Completion condition

`src/greeting.js` exports the exact string `Welcome`.

## Constraints

- Do not create or modify tests.
- Do not perform Git writes.
