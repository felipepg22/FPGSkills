{
  "$schema": "./agent.schema.json",
  "id": "task-executor",
  "displayName": "Task Executor",
  "version": "2.0.0",
  "description": "Execute one specified task only on explicit user request with an authoritative source. Before dispatch, use task-executor-routing for cheap model selection and bounded retries.",
  "manualInvocationOnly": true,
  "defaultModel": "cheap",
  "targets": [
    "codex",
    "opencode",
    "cursor",
    "claude-code",
    "antigravity",
    "generic"
  ]
}
